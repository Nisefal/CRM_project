<Window x:Class="Version_3.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="500" Width="400" ResizeMode="NoResize" WindowStyle="ToolWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="219*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        <Frame Name="Rect" Source="Settings1.xaml" NavigationUIVisibility="Hidden" HorizontalContentAlignment="Left" Margin="2,0,0,3" VerticalContentAlignment="Top" Grid.Column="1" />
        <Button Content="" HorizontalAlignment="Right" VerticalContentAlignment="Top" Width="30" Grid.Column="2" Click="Move_Click" Grid.RowSpan="2"/>
        <Button Content="" HorizontalAlignment="Left" VerticalContentAlignment="Top" Width="30" Click="Move_Click" Grid.RowSpan="2"/>
        <Button Content="Save" HorizontalAlignment="Left" Height="38" Margin="104,1,0,0" VerticalAlignment="Top" Width="125" Grid.Column="1" Click="Save_Click" Grid.Row="1"/>

    </Grid>
</Window>
